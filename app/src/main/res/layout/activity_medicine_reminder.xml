<?xml version="1.0" encoding="UTF-8"?>

    <LinearLayout android:padding="10dp" android:gravity="center"
    android:backgroundTintMode="screen" android:backgroundTint="@color/white"
    android:orientation="vertical" tools:context=".MainActivity"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Medicine Details"
        android:textColor="@color/teal_700"
        android:textSize="25dp"
        android:layout_marginBottom="10dp"
        android:textStyle="bold" />

    <EditText android:layout_height="41dp"
        android:layout_width="391dp"
        android:textStyle="bold"
        android:textSize="20dp"
        android:textColor="@color/black"
        android:id="@+id/editText"
        android:hint="Medicine Name"/>

    <TimePicker android:layout_height="360dp"
        android:layout_width="310dp"
        android:id="@+id/timePicker"
        android:outlineSpotShadowColor="@color/white"
        android:numbersTextColor="@color/black"
        android:outlineAmbientShadowColor="@color/purple_200"
        android:numbersBackgroundColor="@color/purple_200"
        android:layout_marginTop="20dp"/>


    <LinearLayout
    android:layout_height="wrap_content"
    android:layout_width="wrap_content"
    android:layout_marginTop="20dp">

        <ImageButton
            android:id="@+id/setBtn"
            android:layout_width="119dp"
            android:layout_height="82dp"
            android:backgroundTint="#00FFFFFF"
            app:srcCompat="@drawable/image_reminder_take_pressed" />

        <ImageButton
            android:id="@+id/cancelBtn"
            android:layout_width="114dp"
            android:layout_height="82dp"
            android:backgroundTint="#00FFFFFF"
            app:srcCompat="@drawable/image_reminder_not_taken" />

</LinearLayout>

</LinearLayout>